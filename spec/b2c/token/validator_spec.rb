# frozen_string_literal: true

RSpec.describe B2c::Token::Validator do
  it "has a version number" do
    expect(B2c::Token::Validator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
